<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Taskk 3</title>
</head>
<body>
    <h1>Tirth Raval <br> (ID 24DEV077 ) </h1>
    <table>
        <% for(let val of data){%>
            <%const fieldnames=Object.keys(val);%>
            <tr>
                <%for(var i=0;i<fieldnames.length;i++){%>
                    <th>
                        <%=fieldnames[i]%>
                    </th>
                    <%}%>
            </tr>
            <%break;%>
            <%}%>
            <% for(let key of data){%>
            <% const fieldValues = Object.values(key);%>
                <tr>
                    <%for(var i = 0;i<fieldValues.length;i++){%>
                        <td>
                            <%=fieldValues[i]%>
                        </td>    
                    <%}%>
                </tr>
            <%}%>
    </table>
    
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task 3</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }
        h1 {
            text-align: center;
            margin-top: 50px;
        }
        table {
            margin: 0 auto;
            border-collapse: collapse;
            width: 80%;
            background-color: white;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        th, td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
        }
        tr:hover {
            background-color: #f5f5f5;
        }
    </style>
</head>
<body>
    <h1>Tirth Raval <br> (ID
         24DEV077 ) </h1>
    <table border="2" cellpadding="5">
        <% for(let val of data){%>
            <%const fieldnames=Object.keys(val);%>
            <tr>
                <%for(var i=0;i<fieldnames.length;i++){%>
                    <th>
                        <%=fieldnames[i]%>
                    </th>
                    <%}%>
            </tr>
            <%break;%>
            <%}%>
            <% for(let key of data){%>
            <% const fieldValues = Object.values(key);%>
                <tr>
                    <%for(var i = 0;i<fieldValues.length;i++){%>
                        <td>
                            <%=fieldValues[i]%>
                        </td>    
                    <%}%>
                </tr>
            <%}%>
    </table>
</body>
</html>